Rendered before/after UI evidence for `dev-3426-muse-dashboard-quotas`.

Orphan branch, images only — deliberately NOT part of the PR diff, so the
artifact audit and the review diff stay clean. Safe to delete once the PR merges.
